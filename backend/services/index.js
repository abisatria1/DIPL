const participantService = require('./participant')
const eventService = {} // mock
const userService = {} // mock
// Service mapping
module.exports = {
    participantService,
    eventService,
    userService
}